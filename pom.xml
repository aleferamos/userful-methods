<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>br.com.alefe</groupId>
    <artifactId>UserFulMethods</artifactId>
    <version>1.0.0</version>

    <dependencies>
        <!-- Dependências do seu SDK -->
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <mainClass>src/Main.java</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>